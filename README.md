GitHub TypeScript Client
========================

Testing
-------

Set a GITHUB_TOKEN environment variable to a personal access token for your GitHub account.

Run `npm install && npm run build && node dist/samples/run.js`